{
    "image_gen_key": {
        "type": "string",
        "default": "",
        "description": "生图 Token",
        "hint": "必填。在 nai.sta1n.cn 申请到的 toUserId。",
        "obvious": true
    },
    "base_url": {
        "type": "string",
        "default": "https://nai.sta1n.cn",
        "description": "生图服务地址"
    },
    "image_style": {
        "type": "string",
        "default": "vertical",
        "description": "默认风格",
        "options": [
            "vertical",
            "comicDoujin",
            "r18",
            "lolita25d",
            "anime",
            "galgame",
            "自定义"
        ],
        "hint": "选「自定义」时使用下方「自定义画师串」，其余风格使用内置画师串。旧配置里的 custom 与「自定义」等价。"
    },
    "image_size": {
        "type": "string",
        "default": "竖图",
        "description": "默认尺寸",
        "hint": "选择生成图片的尺寸和分辨率；选项中的数字表示相对成本，2K 和 4K 会产生更高成本。",
        "options": [
            "竖图",
            "横图",
            "方图",
            "2K竖图",
            "2K横图",
            "2K方图",
            "4K竖图",
            "4K横图",
            "4K方图"
        ]
    },
    "image_count": {
        "type": "int",
        "default": 2,
        "description": "默认生成数量",
        "hint": "每次生成 1-6 张图片。",
        "min": 1,
        "max": 6
    },
    "bot_reply_mode": {
        "type": "string",
        "default": "完整",
        "description": "Bot 回复模式",
        "hint": "「仅图片」只在成功后发送图片，失败报错不受影响；「简洁」只发送正在画图和简要参数；「完整」保留提示词及参数回复。",
        "options": [
            "仅图片",
            "简洁",
            "完整"
        ]
    },
    "save_image_history": {
        "type": "bool",
        "default": false,
        "description": "保存生图历史",
        "hint": "开启后将所有成功生成的图片保存到 plugin_data/astrbot_plugin_nai_image/image_history。"
    },
    "save_generation_parameters": {
        "type": "bool",
        "default": false,
        "description": "保存生图参数",
        "hint": "配合「保存生图历史」使用。开启后为每张图片保存同名 YAML 文档，记录实际生图参数；不会保存 Token，提示词换行会在生图前转为逗号。"
    },
    "image_history_limit": {
        "type": "int",
        "default": 0,
        "description": "历史保留数量",
        "hint": "仅在保存生图历史时生效。每次保存后自动删除最旧图片及其同名参数文档，仅保留指定数量；设为 0 不清理。",
        "min": 0
    },
    "custom_artists": {
        "type": "string",
        "default": "",
        "description": "自定义画师串",
        "hint": "仅在「默认风格」选择「自定义」时生效。"
    },
    "enable_template": {
        "type": "bool",
        "default": true,
        "description": "启用提示词模板",
        "hint": "开启后将「角色核心关键词与身体描述补全」拼接到提示词最前面；关闭后只发送原始提示词。"
    },
    "character_preset": {
        "type": "string",
        "default": "",
        "description": "角色预设",
        "hint": "填入角色在 NAI 里的核心关键词和身体描述补全，如 muelsyse(Arknights), mechanical headwear 等。无论是否开启 LLM 转译，本项都会拼接（这是关闭转译后唯一保留的提示词预处理）。若源请求插件已带角色词则无需配置。"
    },
    "default_outfit": {
        "type": "string",
        "default": "",
        "description": "默认服装",
        "hint": "在转译模式为「开启」，或「自动」识别到自然语言时生效；提示词没有具体服装且缓存为空时自动补全。使用英文逗号分隔 SD/NAI tag，例如：modern fashion, dark green knit dress, fitted sweater, slim pants, leather boots。"
    },
    "outfit_cache_ttl_seconds": {
        "type": "int",
        "default": 3600,
        "description": "服装缓存时长",
        "hint": "单位为秒，在「开启」模式或「自动」识别到自然语言时生效。具体服装或换装描述会写入缓存，后续模糊描述自动沿用；默认 3600 秒，超时后回退默认服装，设为 0 可关闭缓存。",
        "min": 0,
        "max": 86400
    },
    "model": {
        "type": "string",
        "default": "nai-diffusion-4-5-full",
        "description": "生图模型"
    },
    "steps": {
        "type": "int",
        "default": 24,
        "description": "采样步数"
    },
    "scale": {
        "type": "int",
        "default": 6,
        "description": "scale"
    },
    "cfg": {
        "type": "float",
        "default": 7.0,
        "description": "CFG Rescale",
        "hint": "对应接口参数 cfg，默认 7，范围 0-30，支持小数和 0 值。",
        "min": 0,
        "max": 30
    },
    "sampler": {
        "type": "string",
        "default": "k_dpmpp_2m_sde",
        "description": "采样器",
        "options": [
            "k_dpmpp_2m_sde",
            "k_dpmpp_2m",
            "k_dpmpp_sde",
            "k_dpmpp_2s_ancestral",
            "k_euler_ancestral",
            "k_euler"
        ]
    },
    "noise_schedule": {
        "type": "string",
        "default": "karras",
        "description": "噪声调度"
    },
    "negative": {
        "type": "string",
        "default": "",
        "description": "反向提示词",
        "hint": "留空时使用插件内置的默认反向提示词。"
    },
    "proxy_port": {
        "type": "int",
        "default": 8765,
        "description": "本地代理端口",
        "hint": "「启用本地代理」开启时生效；修改后需要重载插件。",
        "min": 1024,
        "max": 65535
    },
    "enable_proxy": {
        "type": "bool",
        "default": true,
        "description": "启用本地 OpenAI 兼容代理",
        "hint": "开启后在 127.0.0.1:<代理端口> 提供 /v1/images 接口，供陪伴插件或其他客户端调用。关闭后不再占用端口；陪伴插件直连（extension API）不受影响。"
    },
    "enable_companion_link": {
        "type": "bool",
        "default": true,
        "description": "启用陪伴插件直连",
        "hint": "开启后为陪伴系列插件暴露与 image_companion 一致的扩展接口（get_nai_image_api / extension_api），生图请求直接在进程内调用本插件，无需经过本地代理。"
    },
    "companion_prompt_format": {
        "type": "string",
        "default": "自然语言模式（en）",
        "description": "陪伴直连提示词模式",
        "hint": "自然语言模式（en）：把陪伴插件发来的英文自然语言（可含背景信息与需求）原样直接提交给 NAI，不经过 LLM 转译，更适合新版 NAI；nai tag模式：提示词按 NovelAI 标签规则归一化后原样透传，更灵活。",
        "options": [
            "自然语言模式（en）",
            "nai tag模式"
        ]
    },
    "companion_image_retention_days": {
        "type": "int",
        "default": 30,
        "description": "陪伴直连图片保留天数",
        "hint": "陪伴直连生成的图片保存在 plugin_data/astrbot_plugin_nai_image/companion_images，按天清理超期文件；设为 0 不按时间清理。",
        "min": 0,
        "max": 3650
    },
    "enable_translate": {
        "type": "string",
        "default": "关闭",
        "description": "提示词转译模式",
        "hint": "「关闭」原样发送提示词；「开启」将整个提示词交给 LLM 转译；「自动」保留 NAI 标签，只提取自然语言片段转译后拼接。自动模式可用 |nl| 显式标记后续自然语言。旧版布尔值会在插件加载时自动迁移为「关闭」/「开启」并写回配置。",
        "options": [
            "关闭",
            "开启",
            "自动"
        ]
    },
    "translate_provider": {
        "type": "string",
        "default": "",
        "description": "转译模型供应商",
        "hint": "从 AstrBot 已配置的 provider 中选一个。留空时使用 AstrBot 的默认 provider。建议选择便宜/快速的小模型。",
        "_special": "select_provider"
    },
    "enable_llm_tool": {
        "type": "bool",
        "default": false,
        "description": "启用 LLM 生图工具",
        "hint": "关闭后 LLM agent 无法调用此工具；开启后允许 LLM 自主决定生图时机（每次限 1 张）。"
    }
}
